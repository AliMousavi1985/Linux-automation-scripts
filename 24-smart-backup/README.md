# 24. Smart Local Backup & Retention

An automated backup script that archives critical directories with timestamps and applies a 7-day retention policy to free up storage.

## 🚀 Usage
\`\`\`bash
chmod +x smart-backup.sh
sudo ./smart-backup.sh
\`\`\`
