# 23. User & Sudo Security Audit

A utility script that audits system accounts, checks for empty passwords, and lists users with root/sudo privileges.

## 🚀 Usage
\`\`\`bash
chmod +x user-audit.sh
sudo ./user-audit.sh
\`\`\`
